requests>=2.31.0
websocket-client>=1.7.0
playwright>=1.40.0
crewai[google-genai]>=1.0.0
litellm>=1.40.0
pydantic>=2.5.0
python-dotenv>=1.0.0
pdfplumber>=0.10.0
chromadb>=0.4.0
langchain-text-splitters>=0.0.1

# OPCIONAL - OCR con GPU (CUDA). No incluido por defecto: pesa ~2GB (torch) y
# requiere NVIDIA. Para activarlo, instalar a mano antes de iniciar el worker:
#   pip install "marker-pdf[gpu]"   (o "marker-pdf" para CPU)
# Sin él, ocr_marker.py usa pdfplumber como fallback automáticamente.
# marker-pdf>=1.0.0
